import Graph.GraphNode;

public class List {
        List data;
        public List<E> prevNode;
        List<E> nextNode;

        public List(List e){
            this.data = e;
            this.prevNode = null;
            this.nextNode = null;
        }
  public void add(GraphNode nextnode){
            if( this.nextNode == null){

            }
                this.nextNode = new List(nextNode);

                }
            else{
                this.nextNode
            }

    }

